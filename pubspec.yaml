name: slabhaul
description: Crappie fishing companion app — find brush piles, check weather, calculate depth.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1

  # Routing
  go_router: ^14.8.0

  # Backend
  supabase_flutter: ^2.8.0

  # Maps
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  flutter_map_marker_cluster: ^1.4.0

  # HTTP & Data
  http: ^1.2.1
  dio: ^5.4.0

  # Charts
  fl_chart: ^0.69.0

  # UI
  google_fonts: ^6.2.1
  shimmer: ^3.0.0

  # Utils
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0
  geolocator: ^13.0.1
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.3

  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # App branding
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

flutter_native_splash:
  color: "#0F172A"
  android: true
  ios: true
  android_12:
    color: "#0F172A"

flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/images/
    - assets/images/baits/
    - .env
